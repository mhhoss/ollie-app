__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
*.egg-info/
.env

# Runtime state — one SQLite file per deployed store, never shared
*.db
*.db-wal
*.db-shm

# Real secrets — config.example.toml is the committed template
config.toml

# Reserved for Phase 7, if the bot ends up caching files locally
# receipts/

#
m1-spec.html
